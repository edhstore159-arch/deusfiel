/**
 * Mantido apenas por compatibilidade com imports antigos.
 * Não deve lançar erro de runtime, porque isso causa tela branca no app.
 */
export const DebugErrorThrower = () => {
  return null;
};

export default DebugErrorThrower;
